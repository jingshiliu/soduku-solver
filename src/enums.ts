export enum CellColor {
    SLATE = 'text-slate-500',
    RED = 'text-red-400',
    BLUE = 'text-blue-400',
}

export enum GameStatus {
    IN_PROGRESS = 'in-progress',
    WON = 'won',
    LOST = 'lost',
    WAITING = 'waiting',
}
